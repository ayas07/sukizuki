<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SukiZuki</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <div class="nav">
          <div class="nav-left">
            <%= link_to image_tag("logo.png", class: "logo"), root_path %>
          </div>
          <% if user_signed_in? %>
            <div class="nav-right">
              <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "nav-logout" %>
              <%= link_to "新規投稿", new_post_path, class: "nav-btn" %>
            </div>
          <% else %>
            <div class="nav-right">
              <%= link_to "ログイン", new_user_session_path, class: "nav-btn" %>
              <%= link_to "新規登録", new_user_registration_path, class: "nav-btn" %>
            </div>
          <% end %>
        </div>
      </header>

      <div class="main">
        <div class="side-bar">
          <%= render "shared/side_bar" %> 
        </div>
        <div class="main-content">
          <%= yield %>
        </div>
      </div>

      <footer class="footer">
        <p class="copyright">Copyright © SukiZuki All rights reserved.</p>
      </footer>
    </div>
  </body>
</html>
